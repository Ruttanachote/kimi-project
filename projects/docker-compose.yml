version: '3.8'

services:
  google-clone:
    image: nginx:alpine
    container_name: google-clone
    ports:
      - "8080:80"
    volumes:
      - ./google-clone:/usr/share/nginx/html:ro
    restart: unless-stopped
    
  # ถ้านายท่านอยากแก้ไขและ rebuild บ่อยๆ ใช้ตัวนี้แทน:
  # google-clone-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: google-clone-dev
  #   ports:
  #     - "8080:80"
  #   volumes:
  #     - ./google-clone:/usr/share/nginx/html:ro
  #   restart: unless-stopped
